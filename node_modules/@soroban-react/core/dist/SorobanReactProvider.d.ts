import { Connector, WalletChain } from '@soroban-react/types';
import React from 'react';
/**
 * @param children - A React subtree that needs access to the context.
 */
export interface SorobanReactProviderProps {
    appName?: string;
    autoconnect?: boolean;
    chains: WalletChain[];
    children: React.ReactNode;
    connectors: Connector[];
}
export declare function SorobanReactProvider({ appName, autoconnect, chains, children, connectors, }: SorobanReactProviderProps): React.JSX.Element;
