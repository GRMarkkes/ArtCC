import * as SorobanClient from 'soroban-client';
export interface contractTransactionProps {
    networkPassphrase: string;
    source: SorobanClient.Account;
    contractAddress: string;
    method: string;
    args?: SorobanClient.xdr.ScVal[];
}
export declare function contractTransaction({ networkPassphrase, source, contractAddress, method, args, }: contractTransactionProps): SorobanClient.Transaction;
