import { SorobanContextType } from '@soroban-react/core';
import * as SorobanClient from 'soroban-client';
import type { TxResponse, Simulation } from './types';
export type TransactionStatus = 'idle' | 'error' | 'loading' | 'success';
export interface contractTransactionProps {
    networkPassphrase: string;
    source: SorobanClient.Account;
    contractId: string;
    method: string;
    params?: SorobanClient.xdr.ScVal[];
}
export declare function contractTransaction({ networkPassphrase, source, contractId, method, params, }: contractTransactionProps): SorobanClient.Transaction;
export interface SendTransactionResult<E = Error> {
    data?: SorobanClient.xdr.ScVal;
    error?: E;
    isError: boolean;
    isIdle: boolean;
    isLoading: boolean;
    isSuccess: boolean;
    sendTransaction: (txn?: Transaction, opts?: SendTransactionOptions) => Promise<(TxResponse & {
        xdr: string;
    }) | Simulation>;
    reset: () => void;
    status: TransactionStatus;
}
type Transaction = SorobanClient.Transaction | SorobanClient.FeeBumpTransaction;
export interface SendTransactionOptions {
    timeout?: number;
    skipAddingFootprint?: boolean;
    secretKey?: string;
    sorobanContext?: SorobanContextType;
}
export declare function useSendTransaction<E = Error>(defaultTxn?: Transaction, defaultOptions?: SendTransactionOptions): SendTransactionResult<E>;
export {};
